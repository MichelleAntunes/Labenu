/*# Exercício 1

No primeiro exercício vamos praticar a criação de arrays. Crie 3 arrays diferentes:

- O primeiro deve possuir apenas **números**, e estes números não devem seguir nenhuma ordem específica;
- O segundo array deve possuir apenas **strings**;
- O terceiro array deve possuir **números**, **strings** e **booleanos**.
Imprima cada um dos arrays acima, utilizando `console.log()`*/

const primeiraArray = [51,20,104,320] //apenas numeros

const segundaArray = ["banana", "maça","uva", "morango"] //somente strings

const boleano = (20===0) //criei uma constante para poder utiliza-la depois. Caso se isso estivesse dentro na Array abaixo, isso nao seria possivel.
const terceiraArray = [29,"Michelle", boleano]
//numeros, strings e boleanos

console.log (primeiraArray)
console.log(segundaArray)
console.log(terceiraArray)

